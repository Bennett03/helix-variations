[{&quot;name&quot;:&quot;main.py&quot;,&quot;content&quot;:&quot;Web VPython 3.2\n\n#try changing the blue numbers lines 11 - 24\n#it&#39;s useful to change just one at a time \n#to see what each does \n\n#change all blue numbers before on lines 11-24\n#before you turn your assignment in \n\n#number of slats\nn = 20\n#frames per second\nframe_rate = 10\n#vector containing width, height, and length of slats\nsize = vector(20, 0.5, 5)\n\n#canvas color vector containing R, G, and B\ncanvas_color = vector(0, 0, 1)\n\n#see if you can figure out what these do by changing them\nseconds_to_helix = 5\nspeed_coefficient = 0.1\nbottom_color = vector(0, 1, 1)\ntop_color = vector(0, 1, 0)\n\n&#39;&#39;&#39;leave the code below alone for now!&#39;&#39;&#39;\n\nscene.background = canvas_color #assign canvas color to scene&#39;s background\nslats = [] #list of slats to populate\nfth = frame_rate * seconds_to_helix #frames to helix\n\nfor i in range (n):\n  slt = box()  #create a new box\n  slt.size = size #assign vector \&quot;size\&quot; to box dimensions\n  slt.speed = random() * speed_coefficient #give box speed in rotation per frame\n  slt.rotate(angle = i/3 - slt.speed * fth, axis = vector(0, 1, 0)) #initial rotation based on position from stack in helix - random offset\n  slt.pos.y = i - n/2 #vertical position based on place in sequence\n  s_clr = vector(0, 0, 0) #intiialize slat color\n  #interpolate slat color between top and bottom color based on position in stack\n  for d in [&#39;x&#39;, &#39;y&#39;, &#39;z&#39;]:\n    s_clr[d] = top_color[d] * i/n + bottom_color[d] * (n-i)/n\n  slt.color= s_clr #assign s_clr to slat&#39;s color\n  #b.speed = 0.01\n  slats.append(slt) #add slat to list of slats\n\nframe = 0 # initialize frames elapsed at 0\nwhile True:\n  rate(frame_rate) # set frames per second\n  for slt in slats: # for each slat in list of slats\n    a = slt.speed #get amount of rotation\n    if frame%(fth*2) &gt;= fth: #reverse rotation each complete cycle\n      a *= -1\n    slt.rotate(angle = a, axis = vector(0, 1, 0)) #rotate slate around y axis\n  frame += 1 #advance to next frame&quot;}]